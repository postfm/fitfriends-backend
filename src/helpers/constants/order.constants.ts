export enum TypeOrder {
  'абонемент' = 'абонемент',
}

export enum TypePay {
  'visa' = 'visa',
  'mir' = 'mir',
  'umoney' = 'umoney',
}

export const ORDER_ALREADY_EXISTS = 'This order already exists!';
